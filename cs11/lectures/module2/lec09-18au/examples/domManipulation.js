(function() {
  "use strict";

  window.addEventListener("load", initialize);

  function initialize() {
    // add event listener for add button
  }

  // add response function to add paragraph with text from input text box
  function addParagraph() {
    // 1. get text input
    // 2. create paragraph with text (if not empty)
    // 3. add to #p-container
    // 4 (extra): if dblclick'd, remove paragraph from page
  }

})();
